<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================================================================
         FOCAL PERSON — TICKETS FOR A PROJECT
         Route: /customer_support/my_projects/<project_id>/tickets
         ================================================================ -->
    <template id="focal_project_tickets_page" name="Project Tickets">
        <t t-call="web.layout">
            <t t-set="head">
                <meta name="viewport" content="width=device-width, initial-scale=1.0" />
                <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
                    rel="stylesheet" />
                <link
                    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"
                    rel="stylesheet" />
                <link rel="stylesheet" href="/customer_support/static/src/css/focal_projects.css?v=5" />
                <script
                    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
            </t>

            <div id="wrap">
                <!-- NAVBAR -->
                <header class="dashboard-header">
                    <div class="header-content">

                        <!-- Mobile hamburger -->
                        <button class="fp-hamburger" id="fpHamburger"
                            onclick="this.classList.toggle('active'); this.closest('.header-content').classList.toggle('nav-open')"
                            aria-label="Toggle navigation">
                            <span></span><span></span><span></span>
                        </button>

                        <div class="header-nav">
                            <a href="/customer_support/support_dashboard" class="nav-link-external">
                                <i class="bi bi-speedometer2"></i>
                                <span>Overview</span>
                            </a>
                            <a href="/customer_support/my_projects" class="nav-link-external">
                                <i class="bi bi-kanban"></i>
                                <span>My Projects</span>
                            </a>
                        </div>
                        <div class="header-right">

                            <!-- Notification Bell -->
                            <div class="notif-wrap me-1">
                                <button class="notif-btn" id="notifBell" title="SLA Alerts">
                                    <i class="bi bi-bell-fill"></i>
                                    <span class="notif-badge d-none" id="notifBadge">0</span>
                                </button>
                                <div class="notif-dropdown" id="notifDropdown">
                                    <div class="notif-header">
                                        <h6><i class="bi bi-bell me-2"></i>SLA Alerts</h6>
                                        <button class="notif-mark-read" id="markAllRead">Mark all
                                            read</button>
                                    </div>
                                    <div class="notif-list" id="notifList">
                                        <div class="notif-empty">
                                            <i class="bi bi-arrow-repeat"
                                                style="font-size:1.5rem;display:block;margin-bottom:.5rem;"></i>
                                            Loading alerts... </div>
                                    </div>
                                </div>
                            </div>

                            <!-- User Profile Dropdown -->
                            <div class="user-profile dropdown">
                                <a class="d-flex align-items-center text-white dropdown-toggle"
                                    href="#" id="fpUserDropdown" data-bs-toggle="dropdown"
                                    aria-expanded="false" style="text-decoration:none; gap:0.6rem;">
                                    <span class="user-name">
                                        <t t-esc="user.name" />
                                    </span>
                                    <img t-att-src="'/web/image/res.users/%s/avatar_128' % user.id"
                                        alt="Avatar" class="user-avatar"
                                        onerror="this.src='/web/static/img/avatar_gray.svg'" />
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end"
                                    aria-labelledby="fpUserDropdown">
                                    <li>
                                        <a class="dropdown-item"
                                            href="/customer_support/support/profile">
                                            <i class="bi bi-person me-2"></i>Edit Profile </a>
                                    </li>
                                    <li>
                                        <hr class="dropdown-divider" />
                                    </li>
                                    <li>
                                        <a class="dropdown-item text-danger"
                                            href="/customer_support/logout_manual">
                                            <i class="bi bi-box-arrow-right me-2"></i>Logout </a>
                                    </li>
                                </ul>
                            </div>

                        </div>
                    </div>
                </header>

                <!-- PAGE HEADER — just the project name -->
                <div class="fpt-page-header">
                    <div class="fpt-project-pill">
                        <a href="/customer_support/my_projects" class="fpt-back-link"
                            title="Back to My Projects">
                            <i class="bi bi-arrow-left"></i>
                        </a>
                        <i class="bi bi-kanban-fill fpt-project-icon"></i>
                        <span class="fpt-project-name">
                            <t t-esc="project.name" />
                        </span>
                        <t t-if="tickets">
                            <span class="fpt-ticket-count">
                                <t t-esc="len(tickets)" /> ticket<t t-if="len(tickets) != 1">s</t>
                            </span>
                        </t>
                    </div>
                </div>

                <!-- TICKET LIST -->
                <div class="fp-content">
                    <t t-if="not tickets">
                        <div class="fp-empty-state">
                            <div class="fp-empty-icon">
                                <i class="bi bi-ticket-x"></i>
                            </div>
                            <h3>No Tickets Assigned</h3>
                            <p>You have no tickets assigned to you in this project yet.</p>
                            <a href="/customer_support/my_projects" class="fp-primary-btn mt-3">
                                <i class="bi bi-arrow-left me-1"></i>Back to Projects </a>
                        </div>
                    </t>

                    <t t-if="tickets">
                        <div class="fp-ticket-list">
                            <t t-foreach="tickets" t-as="ticket">
                                <a t-attf-href="/customer_support/ticket/{{ ticket.id }}/board"
                                    class="fp-ticket-row">

                                    <!-- Priority side bar -->
                                    <div
                                        t-attf-class="fp-priority-bar fp-priority-{{ ticket.priority }}"></div>

                                    <!-- Ticket info -->
                                    <div class="fp-ticket-main">
                                        <div class="fp-ticket-top">
                                            <span class="fp-ticket-number">
                                                <i class="bi bi-hash"></i>
                                                <t t-esc="ticket.name" />
                                            </span>
                                            <span
                                                t-attf-class="fp-status-badge fp-status-{{ ticket.state }}">
                                                <t t-if="ticket.state == 'new'"><i
                                                        class="bi bi-circle me-1"></i>New</t>
                                                <t t-if="ticket.state == 'assigned'"><i
                                                        class="bi bi-person-check me-1"></i>Assigned</t>
                                                <t t-if="ticket.state == 'in_progress'"><i
                                                        class="bi bi-arrow-repeat me-1"></i>In
                                                    Progress</t>
                                                <t t-if="ticket.state == 'pending'"><i
                                                        class="bi bi-pause-circle me-1"></i>Pending</t>
                                                <t t-if="ticket.state == 'resolved'"><i
                                                        class="bi bi-check-circle me-1"></i>Resolved</t>
                                                <t t-if="ticket.state == 'closed'"><i
                                                        class="bi bi-lock me-1"></i>Closed</t>
                                            </span>
                                            <span
                                                t-attf-class="fp-priority-badge fp-pri-{{ ticket.priority }}">
                                                <t t-if="ticket.priority == 'urgent'">
                                                    <i class="bi bi-exclamation-triangle-fill me-1"></i>
                                                </t>
                                                <t t-if="ticket.priority == 'high'">
                                                    <i class="bi bi-arrow-up me-1"></i>
                                                </t>
                                                <t t-esc="ticket.priority.title()" />
                                            </span>
                                        </div>
                                        <h4 class="fp-ticket-subject">
                                            <t t-esc="ticket.subject" />
                                        </h4>
                                        <div class="fp-ticket-meta">
                                            <span class="fp-meta-item">
                                                <i class="bi bi-person-circle"></i>
                                                <t
                                                    t-esc="ticket.customer_id.name if ticket.customer_id else 'Unknown'" />
                                            </span>
                                            <span class="fp-meta-sep">·</span>
                                            <span class="fp-meta-item">
                                                <i class="bi bi-calendar3"></i>
                                                <t
                                                    t-esc="ticket.create_date.strftime('%b %d, %Y') if ticket.create_date else 'N/A'" />
                                            </span>
                                            <t t-if="ticket.sla_deadline">
                                                <span class="fp-meta-sep">·</span>
                                                <span
                                                    t-attf-class="fp-meta-item fp-sla-chip fp-sla-{{ ticket.sla_status }}">
                                                    <i class="bi bi-clock-fill"></i> SLA <t
                                                        t-esc="ticket.sla_deadline.strftime('%b %d')" />
                                                </span>
                                            </t>
                                        </div>
                                    </div>

                                    <!-- Right: open board CTA -->
                                    <div class="fp-ticket-cta">
                                        <div class="fp-cta-btn">
                                            <i class="bi bi-kanban-fill"></i>
                                            <span>Board</span>
                                        </div>
                                        <i class="bi bi-chevron-right fp-row-arrow"></i>
                                    </div>
                                </a>
                            </t>
                        </div>
                    </t>
                </div>
            </div>

            <!-- ── NOTIFICATION BELL JS ── -->
            <script type="text/javascript">
                //<![CDATA[
            (function () {
                var bell     = document.getElementById('notifBell');
                var dropdown = document.getElementById('notifDropdown');
                var badge    = document.getElementById('notifBadge');

                bell.addEventListener('click', function (e) {
                    e.stopPropagation();
                    dropdown.classList.toggle('open');
                });
                document.addEventListener('click', function () {
                    dropdown.classList.remove('open');
                });
                dropdown.addEventListener('click', function (e) { e.stopPropagation(); });

                document.getElementById('markAllRead').addEventListener('click', function () {
                    fetch('/customer_support/dashboard/sla_alerts/mark_read', {
                        method: 'POST', credentials: 'same-origin',
                        headers: { 'X-Requested-With': 'XMLHttpRequest' }
                    })
                    .catch(function () {})
                    .then(function () { loadSlaAlerts(); });
                });

                function loadSlaAlerts() {
                    fetch('/customer_support/dashboard/sla_alerts', {
                        method: 'GET', credentials: 'same-origin',
                        headers: { 'Accept': 'application/json' }
                    })
                    .then(function (r) { if (!r.ok) throw new Error(r.status); return r.json(); })
                    .then(function (data) { renderAlerts(data.alerts || []); })
                    .catch(function () { renderAlerts([]); });
                }

                function renderAlerts(alerts) {
                    var list = document.getElementById('notifList');
                    if (!alerts.length) {
                        list.innerHTML = '<div class="notif-empty"><i class="bi bi-check-circle-fill" style="font-size:2rem;color:#10b981;display:block;margin-bottom:.5rem;"></i>All tickets are on track!</div>';
                        badge.classList.add('d-none');
                        return;
                    }
                    badge.textContent = alerts.length;
                    badge.classList.remove('d-none');
                    list.innerHTML = alerts.map(function (a) {
                        var isAssignment = a.alert_type === 'assignment';
                        var cls   = isAssignment ? 'at-risk' : (a.sla_status === 'breached' ? 'breached' : 'at-risk');
                        var icon  = isAssignment ? 'bi-ticket-detailed-fill' : (cls === 'breached' ? 'bi-x-octagon-fill' : 'bi-exclamation-triangle-fill');
                        var label = isAssignment ? 'New Assignment' : (cls === 'breached' ? 'SLA Breached' : 'At Risk');
                        var policyLine = isAssignment ? 'Ticket assigned to you' : ('Policy: ' + (a.policy_name || 'None'));
                        return '<div class="notif-item unread">'
                            + '<div class="notif-icon-wrap ' + cls + '"><i class="bi ' + icon + '"></i></div>'
                            + '<div class="notif-body">'
                            + '<div class="notif-title">Ticket #' + a.ticket_id + ' \u2014 ' + a.subject + '</div>'
                            + '<div class="notif-sub">' + label + ' \u00b7 ' + a.time_display + '</div>'
                            + '<div class="notif-time-text">Project: ' + (a.project_name || 'General') + '</div>'
                            + '</div><div class="notif-dot ' + cls + '"></div></div>';
                    }).join('');
                }

                document.addEventListener('DOMContentLoaded', function () {
                    loadSlaAlerts();
                    setInterval(loadSlaAlerts, 30000);
                });
            })();
            //]]>
            </script>

        </t>
    </template>
</odoo>